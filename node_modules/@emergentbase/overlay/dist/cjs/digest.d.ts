type AnyNode = Record<string, any>;
export declare function buildReplayDigest(report: {
    note?: string;
    url?: string;
    error?: Record<string, unknown> | null;
    recorder?: string;
    events?: AnyNode[];
}): string;
export {};
